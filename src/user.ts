/**
 * This file serves as a template/example for isolating endpoint handlers
 * into separate files. Use this pattern to organize and modularize other
 * API endpoints in a similar way.
 */

import { Request, Response } from "express";

export const getUser = (req: Request, res: Response) => {
  console.log("Not implemented");
  res.send("User not implemented");
};
